const people: string[] = []
//const salary: number[] = []
const salary : Array<number>= []

type User = {
    name: string,
    isActive: boolean
}
const allUsers : User[] =[]

const MLModels: number[][]= [
    [255,255,255],
    []
]

people.push('hari')

allUsers.push({name:'', isActive: true})
console.log(allUsers);
